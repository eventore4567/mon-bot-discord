name: SentriX Final Stability Guard

on:
  push:
    branches: [main]
    paths:
      - "railway_boot.py"
      - "cogs/final_stability_guard.py"
      - "cogs/no_cooldown_final.py"
      - "cogs/logs_unified_v6.py"
      - "tests/test_final_stability_guard.py"
      - ".github/workflows/final-stability-guard.yml"
  pull_request:
    paths:
      - "railway_boot.py"
      - "cogs/final_stability_guard.py"
      - "cogs/no_cooldown_final.py"
      - "cogs/logs_unified_v6.py"
      - "tests/test_final_stability_guard.py"
      - ".github/workflows/final-stability-guard.yml"

jobs:
  final-stability:
    runs-on: ubuntu-latest
    timeout-minutes: 8
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.12"
          cache: pip
      - name: Install runtime dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Compile guarded runtime
        run: python -m py_compile railway_boot.py cogs/final_stability_guard.py cogs/no_cooldown_final.py cogs/logs_unified_v6.py
      - name: Verify final stability contracts
        env:
          DISCORD_TOKEN: "ci.fake.token"
          DATABASE_PATH: "/tmp/sentrix-final-stability.db"
        run: PYTHONPATH=. python tests/test_final_stability_guard.py
