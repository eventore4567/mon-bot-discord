name: SentriX Discord UI
on:
  pull_request:
  push:
    branches: [main]
  workflow_dispatch:

jobs:
  discord-ui:
    runs-on: ubuntu-latest
    timeout-minutes: 10
    steps:
      - uses: actions/checkout@v4
      - uses: actions/setup-python@v5
        with:
          python-version: "3.11"
          cache: pip
      - run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
      - name: Compile authoritative runtime
        run: |
          python -m compileall -q \
            utils/embeds.py \
            utils/log_service.py \
            utils/helpers.py \
            cogs/help.py \
            cogs/logs.py \
            cogs/error_experience_v3.py \
            cogs/command_error_release_v41.py \
            cogs/__init__.py
      - name: Run Discord UI contracts
        run: PYTHONPATH=. python tests/test_discord_ui_contract.py
        env:
          DISCORD_TOKEN: "ci.fake.token"
          DATABASE_PATH: "/tmp/sentrix-discord-ui.db"
